export const layerIndices = [0, 1, 2, 3] as const;
export const faceIndices = [0, 1, 2, 3] as const;

export const treeOrnamentsConfig = {
  minPerFace: 2,
  maxPerFace: 8,
  sizeMin: 25,
  sizeMax: 50,
  triangleHeight: 300,
  triangleHalfWidth: 150,
  yMin: 40,
  yMax: 255,
  yMaxLimit: 260,
  edgePadding: 6,
} as const;
